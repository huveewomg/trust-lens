[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd my-fastapi-app && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"